import styled from "styled-components";

const ContentsContainer = styled.div`
  color: #ffffff;

  display: flex;
  flex-direction: column;
  align-items: center;
  height: 3500px;

  position: relative;
`;

export default {
  ContentsContainer,
};
